# Prometheus metrics configuration
PROMETHEUS_PORT=8000
PROMETHEUS_HOST=0.0.0.0

# Logging level
LOG_LEVEL=INFO

# For production deployment, consider:
# PROMETHEUS_HOST=127.0.0.1  # Only listen on localhost
# PROMETHEUS_PORT=9090       # Use standard Prometheus port